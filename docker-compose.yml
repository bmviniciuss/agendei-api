version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: 'gym-manager-user'
      POSTGRES_PASSWORD: 'gym-manager-pass'
    ports:
      - '15432:5432'
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: